const allAirports = require('../data/airports/all-airports.json');
console.log(allAirports[0]);

const filtredAirports = require('../data/airports/filtred-airports.json');
console.log(filtredAirports[0]);

const missingAirports = require('../data/airports/missing-airports.json');
console.log(missingAirports[0]);

const allCountries = require('../data/countries/all-countries.json');
console.log(allCountries[0]);

const filtredCountries = require('../data/countries/filtred-countries.json');
console.log(filtredCountries[0]);

const missingCountries = require('../data/countries/missing-countries.json');
console.log(missingCountries[0]);

const someData = require('../data/processed/avia_par_mk_PAS_BRD.json');
console.log(someData[0]);